// Ex01_01.cpp - A complete C++ program that prints "Hello World!" to the console.
import std; // This line makes the entire C++ standard library available,
            // including the std::println() functionality used below.

int main() {
  int answer {42}; // Defines the variable 'answer' and initializes it to 42.
  std::println("The answer to life, the universe, and everything is {}.", answer);
  return 0;
}
